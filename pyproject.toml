[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "octopus-aiida"
version = "0.1.0"
description = "AiiDA plugin for running and parsing Octopus calculations"
readme = "README.md"
requires-python = ">=3.10"
license = { file = "LICENSE" }
authors = [
    { name = "Alexander Buccheri", email = "alexander.buccheri@mpsd.mpg.de" },
]
keywords = ["AiiDA", "Octopus", "electronic structure", "materials science"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Framework :: AiiDA",
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Topic :: Scientific/Engineering",
]
dependencies = [
    "aiida-core>=2.0,<3",
    "postopus>=0.5.0,<0.6",
]

[project.entry-points."aiida.calculations"]
"octopus.ground_state" = "octopus_aiida.calculations.ground_state:OctopusGSCalculation"

[project.entry-points."aiida.parsers"]
"octopus.ground_state" = "octopus_aiida.parsers.ground_state:OctopusGSParser"

[tool.flit.module]
name = "octopus_aiida"
